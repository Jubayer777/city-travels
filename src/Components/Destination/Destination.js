import React from 'react';
import Header from '../Header/Header';

const Destination = () => {
    return (
        <div>
            <Header></Header>
            <div style={{textAlign:'center',marginTop:'60px'}}>
                <h1>Destination page is under Development...</h1>
            </div>
        </div>
    );
};

export default Destination;